# ── Core servers ──────────────────────────────────────────────────────────────
flask>=3.0.0          # Fog server, Cloud server, SDN Proxy monitoring API
fastapi>=0.109.0      # API Gateway
uvicorn>=0.27.0       # ASGI server for FastAPI

# ── HTTP client (API Gateway → Fog/Cloud/Proxy async calls) ───────────────────
aiohttp>=3.9.1
requests>=2.31.0

# ── Ryu SDN Controller ────────────────────────────────────────────────────────
# Install separately — requires Python 3.9 or 3.10 (not 3.12)
# pip install ryu
#
# If ryu is unavailable for your Python version:
# Use simulation mode instead:  ./run_all.sh

# ── Mininet ───────────────────────────────────────────────────────────────────
# Install via OS package manager (not pip):
# sudo apt-get install mininet
